import { Card } from "@/components/Card";

export default function Page() {
  return (
    <Card title="Settings">
      <div className="p-6 text-sm text-muted">Reset paper account, configure default symbols &mdash; coming step 8</div>
    </Card>
  );
}
